node_modules/
.env
.DS_Store
.tmp/
tmp/
raw/
coverage/
digest/private-*.md
data/private-*.json
# Per-install cross-run dedup state. Must NOT be shipped: a committed state
# would pre-mark every item "seen" and make a fresh user's first digest empty.
state-feed.json
